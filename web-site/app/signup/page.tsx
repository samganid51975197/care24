"use client";
import {useState} from 'react';
export default function Signup(){const[message,setMessage]=useState(''),[done,setDone]=useState(false),[busy,setBusy]=useState(false);
 async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const b=Object.fromEntries(new FormData(e.currentTarget));if(b.password!==b.confirm){setMessage('비밀번호 확인이 일치하지 않습니다.');return;}setBusy(true);try{const r=await fetch('/api/auth/signup',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(b)});const d=await r.json();if(!r.ok)throw Error(d.error);setDone(true);setMessage(d.message);}catch(e){setMessage(e instanceof Error?e.message:'신청하지 못했습니다.');}finally{setBusy(false);}}
 return <main className="auth-page"><section className="auth-card"><h1>간병24 가입 신청</h1><p>관리자가 역할과 병원 소속을 확인한 뒤 승인합니다.</p>{!done&&<form onSubmit={submit}><label>이름<input name="name" required maxLength={80} autoComplete="name"/></label><label>아이디<input name="username" required minLength={4} maxLength={32} pattern="[a-zA-Z0-9][a-zA-Z0-9_.\-]{3,31}" autoComplete="username"/><small>영문·숫자와 . _ - 사용, 4~32자</small></label><label>비밀번호<input name="password" type="password" required minLength={12} maxLength={128} autoComplete="new-password"/><small>12자 이상, 다른 사이트와 다른 비밀번호</small></label><label>비밀번호 확인<input name="confirm" type="password" required minLength={12} maxLength={128} autoComplete="new-password"/></label><label>신청 역할<select name="role"><option value="caregiver">간병인</option><option value="hospital">병원 담당자</option></select></label><label>소속 또는 근무 예정 병원<input name="hospital" required maxLength={120}/></label><button disabled={busy}>{busy?'신청 중…':'가입 승인 신청'}</button></form>}<p role="status">{message}</p><a href="/login">로그인으로 돌아가기</a></section></main>;
}
